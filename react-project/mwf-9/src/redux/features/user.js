import { createSlice } from '@reduxjs/toolkit';

let userSlice = createSlice({
    name: "users",
    initialState: {loginUser, signupUser},
    reducers:{
        signUp:(state, action)=>{
            
        }
    }
})
export default userSlice.reducer;
