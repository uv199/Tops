let arr = [1, 2, 3, 4, 5, 1, 2];
console.log("-----------  arr----------->", arr);

/*

// concat(...items) – returns a new array: copies all members of the current one and adds items to it. If any of items is an array, then its elements are taken.

let arr2 = [6, 7, 8, 9];
let arr3 = [10, 11, 12];

let combineArr = arr.concat(arr2, arr3, 100);
console.log("-----------  combineArr----------->", combineArr);

// indexOf/lastIndexOf(item, pos) – look for item starting from position pos, return the index or -1 if not found.

// let index = arr.indexOf(3); // 2
// let index = arr.indexOf(30); // -1
// let index = arr.indexOf({ name: "uv" }); // non primitive
// console.log("-----------  index----------->", index);

// let indexOf = arr.indexOf(1);
let indexOf = arr.indexOf(10);
let lastIndex = arr.lastIndexOf(1);
console.log("-----------  indexOf----------->", indexOf);
console.log("-----------  lastIndex----------->", lastIndex);


// includes(value) – returns true if the array has value, otherwise false.

// let isAvailable = arr.includes(1) //  true 
let isAvailable = arr.includes(11) // false
console.log("-----------  isAvailable----------->", isAvailable)

// sort(func) – sorts the array in-place, then returns it.
let sarr = [1, 2, 4, 5, 11, 22, 9, 4];
console.log("-----------  sarr----------->", sarr)
sarr.sort();
console.log("-----------  sarr----------->", sarr)


// reverse() – reverses the array in-place, then returns it.
let rArr = [1, 2, 3, 4, 5];
console.log("-----------  rArr----------->", rArr)
rArr.reverse();
console.log("-----------  arr----------->", rArr);


// join – convert a array to string with substring.

let arrStr1 = arr.join("--");
let arrStr2 = arr.toString();
console.log("----------- join  arrStr1----------->", typeof arrStr1, arrStr1);
console.log("--------toString arrStr2-------->", typeof arrStr2, arrStr2);

*/
