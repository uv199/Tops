let str = "Hello World..!";
console.log("str", str);
/*
// charAt()	It provides the char value present at the specified index.
let ch = str.charAt(0);
console.log("ch", str.length, ch, str[0]);


// concat()	It provides a combination of two or more strings.
let str2 = "--URVISH";
let str3 = str.concat(str2,str2," tets---");
console.log("str3", str3);

// indexOf()	It provides the position of a char value present in the given string.

let index = str.indexOf("World"); // 2
// let index = str.indexOf("world"); // -1
// let index = str.indexOf("llow"); // -1
console.log("index", index);


// replace()	It replaces a given string with the specified replacement.

let str2 = str.replace("l", " URVISH-->");
// let str2 = str.replaceAll("l", " URVISH-->");
// let str2 = str.replace("eworld", " URVISH-->");
console.log("str2", str2);


// substring()	It is used to fetch the part of the given string on the basis of the specified index.
let subStr = str.substring(1, 5);
console.log("subStr", subStr);

// slice()	It is used to fetch the part of the given string. It allows us to assign positive as well negative index.
// let sliceStr = str.slice(1, 5);
let sliceStr = str.slice(1, -5); // accept negative index
console.log("sliceStr", sliceStr);


// toLowerCase()	It converts the given string into lowercase letter.
let lower = str.toLowerCase();
console.log("lower", lower);

// toUpperCase()	It converts the given string into uppercase letter.
let uper = str.toUpperCase();
console.log("uper", uper);


// toString()	It provides a string representing the particular object.
let x = 11242342;
console.log("x", typeof x, x);
let xStr = x.toString();
console.log("xStr", typeof xStr, xStr);

// split()	It splits a string into substring array, then returns that newly created array.

let splitStr = "abcd abcd test abc";
// let splitStr = "bcdabcdtestabc";

let arr = splitStr.split(" ");
console.log("arr", arr);


// trim()	It trims the white space from the left and right side of the string.

let email = "    uvpatel@gmail.com    ";
console.log("email", email.length);
let x = email.trim();
console.log("x", x.length);

*/
