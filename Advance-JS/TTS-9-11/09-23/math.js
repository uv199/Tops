/*
// ceil()	It returns a smallest integer value, greater than or equal to the given number.
console.log("ceil()", Math.ceil(12.234));
console.log("ceil()", Math.ceil(12.0));

// floor()	It returns largest integer value, lower than or equal to the given number.
console.log("floor()", Math.floor(12.120));


// trunc()	It returns an integer part of the given number.

console.log("trunc()", Math.trunc(12.19089082));


// max()	It returns maximum value of the given numbers.

console.log("max()", Math.max(12.1, 12, 23, 45.78, 45, 9));

// min()	It returns minimum value of the given numbers.

console.log("min()", Math.min(12.1, 0.4, 12, 23, 45.78, 45, 9));

// round()	It returns closest integer value of the given number.
console.log("round()", Math.round(12.43457));
console.log("round()", Math.round(12.53457));

// random()	It returns random number between 0 (inclusive) and 1 (exclusive).

console.log("Math.random()", Math.random());
let x = Math.random() * 10;
console.log("Math.random()", Math.trunc(x));

function generate(params) {
    let x = 0;
    for (let x = 0; x < array.length; x++) {
        // x
        if (x < 6) {
            break;
        }
    }
    console.log(x);
}

let no = generate();
generate();
generate();

*/
