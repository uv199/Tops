let arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];
console.log("-----------  arr----------->", arr);

/*
// push(...items) – adds items to the end,

// arr.push(10)
arr.push(10,11)
console.log("-----------  arr----------->", arr)

// pop() – extracts an item from the end,

arr.pop()
console.log("-----------  arr----------->", arr)

// shift() – extracts an item from the beginning,

arr.shift()
console.log("-----------  arr----------->", arr)

// unshift(...items) – adds items to the beginning.
// arr.unshift(0.5)
arr.unshift(0,0.5)
console.log("-----------  arr----------->", arr)


// concat(...items) – returns a new array: copies all members of the current one and adds items to it. If any of items is an array, then its elements are taken.

let arr2 = [11, 12];
let arr3 = [13, 14];

// let newArr = arr.concat(arr2);
// let newArr = arr.concat(arr2, arr3);
let newArr = arr.concat(arr2, arr3, 15);
console.log("-----------  newArr----------->", newArr);


// sort(func) – sorts the array in-place, then returns it.
let arr2 = [1, 2, 22, 0.5,4, 2, 33, 34, 3];

arr2.sort()
console.log("-----------  arr2----------->", arr2)

// reverse() – reverses the array in-place, then returns it.

arr.reverse()
console.log("-----------  arr----------->", arr)

// indexOf/lastIndexOf(item, pos) – look for item starting from position pos, and return the index or -1 if not found.
let arr2 = [1, 2, 3, 1, 6, 7];
// let index = arr2.indexOf(1);
// let index = arr2.indexOf(10);
let index = arr2.lastIndexOf(1);
console.log("-----------  index----------->", index)

let arr2 = [{ a: 20 }, { a: 30 }];
let index = arr2.indexOf({ a: 20 });
console.log("-----------  index----------->", index)



// let slicedArr = arr.slice(1,4)
let slicedArr = arr.slice(1,-4)
console.log("-----------  slicedArr----------->", slicedArr)

*/
