// const sub_sum = require("./sum-sub");
const { sub, sum } = require("./sum-sub");
console.log("sub, sum ", sub, sum )
// console.log("sub_sum", sub_sum);

// let deiffernce = sub_sum.sub(10, 20);
let deiffernce = sub(10, 20);
console.log("deiffernce", deiffernce);
