let str = "Hello World,";
console.log("-----------  str----------->", str);
/*
// concat()	It provides a combination of two or more strings.

let name = "Urvish"

let str2 = str.concat(name," Patel")
console.log("-----------  str2----------->", str2)


// indexOf() It provides the position of a char value present in the given string.

let index = str.indexOf("ord")
console.log("-----------  index----------->", index)

// lastIndexOf()	It provides the position of a char value present in the given string by searching a character from the last position.
let index2 = str.lastIndexOf("o")
console.log("-----------  index2----------->", index2)

// replace()	It replaces a given string with the specified replacement.

let replaceStr = str.replace("o", "*");
console.log("-----------  replaceStr----------->", replaceStr)

// replaceAll()
let replaceStrAll = str.replaceAll("o", "*");
console.log("-----------  replaceStrAll----------->", replaceStrAll)

// substring()	It is used to fetch the part of the given string on the basis of the specified index.

let subStr = str.substring(1,6)
console.log("-----------  subStr----------->", subStr.length)

// slice()	It is used to fetch the part of the given string. It allows us to assign positive as well negative index.

let subSlice = str.slice(1,-1)
console.log("-----------  subSlice----------->", subSlice)

// toLowerCase() It converts the given string into lowercase letter.

let lower= str.toLowerCase()
console.log("-----------  lower----------->", lower)


// toUpperCase() It converts the given string into uppercase letter.
let upper = str.toUpperCase();
console.log("-----------  upper----------->", upper);
let database = "urvish";
let input = "UrvIsh";

if (database.toLowerCase() === input.toLowerCase()) {
    console.log("-----both are match---->");
} else {
    console.log("-----both are diffrent---->");
}


// split()	It splits a string into substring array, then returns that newly created array.

let arr = str.split("l")
console.log("-----------  arr----------->", arr)

*/
// trim() It trims the white space from the left and right side of the string.


// includes()
let ans = str.includes("l0")
console.log("-----------  ans----------->", ans)

// harshil -> indexof() =-> 100
