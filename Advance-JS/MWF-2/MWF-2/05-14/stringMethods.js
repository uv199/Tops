let str = "Hello World";
console.log("-----------  str----------->", str)

/*

// concat()	It provides a combination of two or more strings.
let str2 = " urvish";
let str3 = str.concat(str2,"...?");
console.log("-----------  str3----------->", str3)

// indexOf()	It provides the position of a char value present in the given string.

// let index = str.indexOf("l")
let index = str.indexOf("L")
// let index = str.indexOf("ello")
console.log("-----------  index----------->", index)


const lastIndex = str.lastIndexOf("ello")
console.log("-----------  lastIndex----------->", lastIndex)



// replace()	It replaces a given string with the specified replacement.

let newStr = str.replaceAll("l","*")
// let newStr = str.replaceAll("ello","*")
// let newStr = str.replace("l","*")
console.log("-----------  newStr----------->", newStr)


// substring()	It is used to fetch the part of the given string on the basis of the specified index.

let subString = str.substring(2, 5);
console.log("-----------  subString----------->", subString);

// slice()	It is used to fetch the part of the given string. It allows us to assign positive as well negative index.
let sliceString = str.slice(2, -2);
console.log("-----------  sliceString----------->", sliceString);

// split()	It splits a string into substring array, then returns that newly created array.

let arr = str.split(" ")
console.log("-----------  arr----------->", arr)

*/

// trim()	It trims the white space from the left and right side of the string.

let str2 = "  te  "
console.log("-----------  str2----------->", str2.length)
let str3= str2.trim()
console.log("-----------  str3----------->", str3.length)
