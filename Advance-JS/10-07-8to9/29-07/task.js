// let name = "Urvish v Patel" //"Patel v Urvish"
// ["Urvish","v","Patel"] // split
// ["Patel","v","Urvish"] // reverse
// "Patel V Urvish"; // join
/*
// let test = "Urvish     v          Patel           " // "urvish v patel"
// "" "" e!
// let test = "Urvish" // "u_r_v_i_s_h"
// let test = "Urvish" // "u-r+v-i+s-h"// even/odd // length===i
// let test = "Urvish v patel" // "Urvish V Patel" //split->map/loop->upperCase
let new Arr =["Urvish" "v" "patel"].map((e)=>{
    e[0]
    return e
})


let str = "urvish";
let newStr = ""
if (i % 2 === 0) {
  if (i === str.length) {
    newStr+=str[i];
  }
  str[i] + "+";
} else {
  if (i === str.length) {
    str[i];
  }
  str[i] + "-";
}
 */

Math.random() * 10;
0.23;
1.23;
9.12312;
Math.floor(0.23);
0;
2;
3;
6;
8;

function name(params) {
  let x = 0;
  for (let i = 0; i > 0; i++) {
    let a = Math.random() * 6 + 1;
    // 0.2*6; 1.12
    // 6,12,18,24,3,6,4,4,5,
    1.23;
    9.12312;
    x = Math.floor(a);
    if (x > 7 && x !== 0) {
      break;
    }
  }
}
