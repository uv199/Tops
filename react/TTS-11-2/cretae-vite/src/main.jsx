import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <h1>test</h1>
    <h1>test</h1>
  </React.StrictMode>
);
/*

const head = <h1>hello world</h1>;

let nameArr = ["urvish", "dwanil", "jayom"];
let nameDiv = nameArr.map((e)=>return h1)

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <div>
      {head}
      {5 + 5}
      {nameArr.map((e, i) => {
        return (
          <div>
            <p>{e}</p>
            <h1>{i}</h1>
          </div>
        );
      })}
    </div>
  </React.StrictMode>
);


*/
